int main() {
    int a = 5;
    int b = 10;
    
    int addition = a + b;
    int subtraction = a - b;
    int multiplication = a * b;
    int division = a / b;
    int modulus = a % b;
    int plus = +a;
    int minus = -a;
    
    write(addition);
    write(subtraction);
    write(multiplication);
    write(division);
    write(modulus);
    write(plus);
    write(minus);
    
    return 0;
}